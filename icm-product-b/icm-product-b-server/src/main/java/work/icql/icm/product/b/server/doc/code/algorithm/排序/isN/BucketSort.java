package work.icql.icm.product.b.server.doc.code.algorithm.排序.isN;

/**
 * 桶排序：
 * 1）算法思想
 * 将要排序的数据分到几个有序的桶里，每个桶里的数据再单独进行排序。
 * 桶内排完序之后，再把每个桶里的数据按照顺序依次取出，组成的序列就是有序的了
 * 2）平均时间复杂度：O(n)
 */
public class BucketSort {

    private BucketSort() {
    }

    //比较适合用于外部排序
}
